% ==== Sprache & Mikrotypografie ====
\usepackage{microtype} % für hübschere Typografie (z.B. Ligaturen)
\usepackage{amsmath} % für erweiterte Mathematik-Umgebungen wie z.B. align

% ==== Fonts & Emoji-Fallback (LuaLaTeX) ====
\usepackage{fontspec}
\usepackage{unicode-math}
\usepackage{luacode}

% TikZ für Diagramme und Grafiken
%\usepackage{tikz}
%\usetikzlibrary{arrows.meta,arrows,3d,bending,positioning}

% Emoji-Fallback (Noto Color Emoji muss installiert sein)
\directlua{
  luaotfload.add_fallback("emojifallback", {
    "Noto Color Emoji:mode=harf;script=DFLT;"
  })
}

% Hauptschrift
\setmainfont{HDA DIN Office Medium}[
  BoldFont    = HDA DIN Office Bold,
  % ItalicFont  = * Italic , % echte Italic, falls vorhanden
  RawFeature  = {fallback=emojifallback}
]
% \setmainfont{Libertinus Serif}[RawFeature={fallback=emojifallback}]
% \setsansfont{Libertinus Sans}[RawFeature={fallback=emojifallback}]
\setmonofont{DejaVu Sans Mono}[Scale=MatchLowercase,RawFeature={fallback=emojifallback}]

\setbeamerfont{frametitle}{size=\large}
% \setbeamerfont{normal text}{size=\small}

% Mathematik
\setmathfont{Libertinus Math}

% Links u. rechts mehr Platz nutzen
\setbeamersize{text margin left=1em, text margin right=1em}

% Footer mit Name, Vorlesungstitel, Logo
\setbeamertemplate{footline}{
  \leavevmode%
  \hbox{%
    \begin{beamercolorbox}[wd=0.3\paperwidth,ht=2.5ex,dp=1.5ex,left]{author in head/foot}
      \usebeamerfont{author in head/foot}\hspace{1em}Prof. Dr.-Ing. Jan Zwiener
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=0.4\paperwidth,ht=2.5ex,dp=1.5ex,center]{title in head/foot}
      \usebeamerfont{title in head/foot}Slidedeck\\ % z.B. durch Titel ersetzen
      Seite \insertframenumber{} / \inserttotalframenumber\hspace{1em}%
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=0.3\paperwidth,ht=2.5ex,dp=1.5ex,right]{date in head/foot}
      \includegraphics[height=7.5ex]{img/hdalogo.png}\hspace{1em}
    \end{beamercolorbox}%
  }%
  \vskip0pt
}

% Git-Hash und Build-Date von Pandoc setzen lassen
\newcommand{\githash}{\githash}     % wird von Pandoc ersetzt
\newcommand{\buildtag}{\buildtag}
